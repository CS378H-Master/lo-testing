// *Good testcase*
// String Operations
// main return value: 1
// migrated 2026-06-03: int sentinel; success iff orig_main() is true
// Author: Tyler Collins


bool orig_main() {
    String a, b, c, d;
    a = "The rain in Spain falls";
    b = (~a);
    return (a < b);
}

int main() {
    return (orig_main() ? 1 : 0);
}
