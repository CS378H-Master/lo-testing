// bad test case
// missing Main class
// expected compile error: E_NO_MAIN_CLASS
// Author: Tyler Collins (Summer 2022)

class room(int length, breadth, height;) [
    room(){
        length = 4;
        breadth = 5;
        height = 6;
    }
] {
    int getLength() {
        return length;
    }
    int getBreadth() {
        return breadth;
    }
}
